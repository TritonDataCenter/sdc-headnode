{
    "port": 80,
    "logLevel": "debug",
    "mode": "dc",
    "database": {
        "type": "ufds",
        "url": "ldaps://{{{UFDS_SERVICE}}}",
        "bindDN": "{{{ufds_ldap_root_dn}}}",
        "bindPassword": "{{{ufds_ldap_root_pw}}}"
    },
    "storage": {
        "local": {
            "dir": "{{{LOCAL_DB_DIR}}}"
        }
    },
    "wfapi": {
        "workflows": [ "create-from-snapshot" ],
        "url": "http://{{{WORKFLOW_SERVICE}}}"
    }
}
