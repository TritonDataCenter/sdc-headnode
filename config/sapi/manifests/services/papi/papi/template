{
    "port": 80,
    "logLevel": "info",
    "moray": {
        "url": "http://{{{MORAY_SERVICE}}}:2020",
        "connectTimeout": 1000,
        "retry": {
            "retries": 3,
            "minTimeout": 1000
        },
        "noCache": true
    },
    "ufds_admin_uuid": "{{{ufds_admin_uuid}}}",
    "ufds_sync": {{{SERVICE_SYNC_UFDS}}},
    "first_boot": {{{SERVICE_IS_FIRST_BOOT}}},
    "ufds": {
        "url": "ldaps://{{{ufds_domain}}}",
        "bindDN": "{{{ufds_ldap_root_dn}}}",
        "bindPassword": "{{{ufds_ldap_root_pw}}}",
        "cache": {
            "size": 5000,
            "expiry": 60
        },
        "clientTimeout": 240000,
        "maxConnections": 1
    },
    "sapi": {
        "url": "http://{{{SAPI_SERVICE}}}"
    },
    "bucket": "sdc_packages",
    "warning": "Please, keep 'is_default' and 'group_name' into schema. Translation happening within the App, given 'default' and 'group' cannot be used as PostgreSQL indexes.",
    "schema": {
        "uuid": {
            "type": "string",
            "unique": true
        },
        "urn": {
            "type": "string",
            "unique": true
        },
        "name": { "type": "string" },
        "owner_uuid": { "type": "string" },
        "active": { "type": "boolean" },
        "vcpus": { "type": "number" },
        "cpu_cap": { "type": "number" },
        "is_default": { "type": "boolean" },
        "group_name": { "type": "string" },
        "description": { "type": "string" },
        "max_lwps": { "type": "number" },
        "max_physical_memory": { "type": "number" },
        "max_swap": { "type": "number" },
        "common_name": { "type": "string" },
        "quota": { "type": "number" },
        "networks": { "type": "string" },
        "version": { "type": "string" },
        "parent": { "type": "string" },
        "traits": { "type": "string" },
        "zfs_io_priority": { "type": "number" },
        "fss": { "type": "number" },
        "cpu_burst_ratio": { "type": "number" },
        "ram_ratio": { "type": "number" },
        "overprovision_cpu": { "type": "number" },
        "overprovision_memory": { "type": "number" },
        "overprovision_storage": { "type": "number" },
        "overprovision_network": { "type": "number" },
        "overprovision_io": { "type": "number" }
    },
    "required": [
        "active",
        "cpu_cap",
        "default",
        "group",
        "description",
        "max_lwps",
        "max_physical_memory",
        "max_swap",
        "name",
        "common_name",
        "quota",
        "version",
        "zfs_io_priority"
    ],
    "immutable": [
        "uuid",
        "name",
        "version",
        "quota",
        "max_swap",
        "max_physical_memory",
        "cpu_cap",
        "max_lwps",
        "zfs_io_priority",
        "vcpus"
    ]
}
