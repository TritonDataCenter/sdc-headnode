create -b
set zonepath=/zones/dhcpd
set brand=joyent
set autoboot=true
set ip-type=exclusive
add fs
set dir=/tftpboot/platform
set special=/usbkey/platform
set type=lofs
add options ro
add options nodevices
end
