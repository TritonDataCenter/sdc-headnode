printf "%-56s" "Creating zone ${ZONENAME}-data..." >>/dev/console
zfs create -o compression=on zones/${ZONENAME}/ca-data
zonecfg -z ${ZONENAME} "add dataset; set name=zones/${ZONENAME}/ca-data; end; verify; commit"
printf "%4s\n" "done" >> /dev/console
