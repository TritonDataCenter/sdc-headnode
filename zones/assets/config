create -b
set zonepath=/zones/assets
set brand=joyent
set autoboot=true
set ip-type=exclusive
add fs
set dir=/assets/datasets
set special=/usbkey/datasets
set type=lofs
add options ro
add options nodevices
end
add net
set physical=assets0
set vlan-id=0
set global-nic=admin
end
