#!/bin/bash

set -e

mkdir -p root/opt/smartdc
cd root/opt/smartdc
git clone git@git.joyent.com:public_api.git pubapi-repo
cd pubapi-repo
git branch --track usb-headnode origin/usb-headnode
git checkout usb-headnode
