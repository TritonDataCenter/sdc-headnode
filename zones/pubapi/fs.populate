#!/bin/bash

set -e

mkdir -p root/opt/smartdc
cd root/opt/smartdc
git clone git@git.joyent.com:public_api.git pubapi-repo
cd pubapi-repo
git checkout 20110210
touch ../pubapi-VERSION
git describe --tags > ../pubapi-VERSION

