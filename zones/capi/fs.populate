#!/bin/bash

set -e

mkdir -p root/opt/smartdc
cd root/opt/smartdc
git clone git@git.joyent.com:customers_api.git -b develop capi-repo
cd capi-repo
# CAPI currently uses master as it's stable HEAD
touch ../capi-VERSION
git describe --tags > ../capi-VERSION
